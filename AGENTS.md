# 프로젝트 행동 수칙 및 영구 준수 원칙

## 절대 원칙: 기존 작업/동작 임의 변경 및 되돌리기 영구 금지 (최우선)
- **사용자가 직접 명시적으로 명령하지 않은 이상, 절대로 이전에 했던 작업을 되돌리거나 변경하지 마십시오.**
- **임의 Git 롤백 명령어 영구 실행 금지**: 빌드/문법 오류가 발생하더라도 `git checkout <file>`, `git restore`, `git reset`, `git revert` 등으로 코드를 되돌리는 행위를 절대 금지합니다. 발생한 오류는 원인을 직접 디버깅하여 해결해야 하며, 작업 코드를 날려서는 안 됩니다.
- **기존 작업 100% 보존**: 이미 완성되어 정상 동작하는 기존 기능(화면, 전송/공유 방식, 데이터, UI 등)은 사용자의 직접적인 변경 지시가 없는 한 100% 보존해야 합니다.
- **격리 수정 원칙**: 새로운 작업이나 최적화 수행 시, 요청받은 대상 영역만 정밀하게 격리하여 수정하고 다른 기능에 사이드 이펙트가 미치지 않도록 주의하십시오.
- **빌드/커밋 전 작업 무결성 전수 검증 의무**: 배포나 빌드 실행 전, 사용자가 요청한 작업 내용이 누락 없이 실제 파일에 온전히 반영되어 있는지 반드시 `git diff` 및 소스 코드를 통해 교차 확인하십시오.

## 플랫폼 원칙: 안드로이드(Android) 전용 앱 (iOS 제외)
- **이 프로젝트는 오직 안드로이드(Android) 플랫폼만 대상**으로 합니다.
- iOS 관련 설정, 코드, 가이드, 플랫폼 등록 등은 완전히 배제하며, 모든 라이브러리 연동, 배포, 테스트, 기능 구현은 100% 안드로이드 기준으로만 진행하십시오.

## [프로젝트 핵심 컨텍스트: '마음카드' 개발 및 UX 원칙]

### 1. 타겟 사용자 (5070 중장년·시니어)
- 대상: 스마트폰 조작이 서툴고 노안이 있는 50~70대 시니어.
- 핵심 가치: 복잡한 기능 탐색을 완전히 배제하고, 앱 실행 후 2번의 터치(2초 컷)로 카카오톡 단톡방에 안부 카드를 전송할 수 있는 극강의 단순함 유지.
- 인터랙션 원칙: 슬라이더·미세 제스처 지양, 큼직한 버튼([-] [+])과 명확한 텍스트 레이블 사용. 한글은 음절 단위로 쪼개지지 않고 어절 단위로 떨어지도록 단어 줄바꿈(keep-all) 필수 적용.

### 2. 기기 환경 및 하드웨어 제약
- 저사양 보급형 단말기 최우선: 메모리 누수 방지, 무거운 백그라운드 작업 및 불필요한 고사양 애니메이션 지양, 이미지 캐싱과 리소스 최적화 철저 관리.
- 디스플레이 확대 모드 대응: 시니어 특성상 단말기 설정의 '화면 크게 보기(디스플레이 확대)' 및 '글자 크기 최대' 설정 사용자가 다수임. 세로 공간 부족으로 인한 레이아웃 오버플로우(RenderFlex Overflow)가 절대 발생하지 않도록 SingleChildScrollView 및 반응형 제약(Flexible/Expanded) 필수 설계.
- 다양한 화면 비율 대응: 작은 구형 폰부터 긴 화면의 최신 폰까지 핵심 액션 버튼(카카오톡 전송 등)이 화면 밖으로 밀려나거나 잘리지 않도록 배치.

### 3. 광고 및 수익화 정책
- 사용자 조작 방해 금지: 카카오톡 전송을 누르는 순간 광고를 띄우지 않음. 전송을 완전히 마치고 앱으로 복귀했을 때만 정적 전면광고 노출.
- 소음 광고 원천 차단: 아침 시간대 단톡방 전송 시 당황하지 않도록 소음과 카운트다운 피로를 유발하는 동영상 광고는 일절 배제(텍스트/이미지/리치 미디어만 송출).
- 배너 광고: 메인 및 결과 화면 모두 시각적 혼동과 레이아웃 붕괴를 막기 위해 화면 하단에 고정되는 얇은 적응형 배너만 사용.
